services:
  - docker

env:
  global:
    - IMAGE_NAME=redpanda/shoppinglist

script:
  - docker build --pull --cache-from "$IMAGE_NAME" --tag "$IMAGE_NAME" .

after_script:
  - docker run --rm "$IMAGE_NAME" npm run test
  - docker images
